package testcases;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.PageFactory;
import org.testng.annotations.Test;

import core.Core;
import pages.homepage;

public class test1 extends Core {
	// WebDriver driver;

	@Test
	public void clickoncategorylink() {
		/*
		 * try {
		 * 
		 * System.setProperty("webdriver.chrome.driver",
		 * ".\\resources\\drivers\\chromedriver.exe"); WebDriver driver = new
		 * ChromeDriver(); driver.get("http://inhousepharmacy.biz/");
		 * driver.manage().window().maximize(); //search for a product in search box
		 * //driver.findElement(By.xpath("//*[@id=\"ctl00_ctl13_tsfSearch_Input\"]")).
		 * sendKeys("Finpecia"); //SearchButton.click();
		 * 
		 * homepage hmepg = new homepage(driver); //hmepg.generallinkclick();
		 * Thread.sleep(50); hmepg.SearchProduct("Finpecia");
		 * 
		 * 
		 * // SearchProduct("Finpecia");
		 * 
		 * hmepg.SearchProduct("Finpecia"); hmepg.searchbuttonclick();
		 * 
		 * 
		 * }
		 * 
		 * catch(Exception e) { e.printStackTrace(); }
		 */

	}
}