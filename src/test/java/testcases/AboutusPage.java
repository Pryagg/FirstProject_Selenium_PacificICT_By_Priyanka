package testcases;

import org.testng.annotations.Test;

import core.Core;
import pages.homepage;

public class AboutusPage extends Core
{
	/*
	 * @Test public void Aboutus() { try { //homepage hmepg = new homepage(driver);
	 * Thread.sleep(50); hmepg.Aboutus(); } catch(Exception e) {
	 * e.printStackTrace();
	 * 
	 * } }
	 */
}
