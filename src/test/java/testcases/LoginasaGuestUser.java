package testcases;

import org.testng.annotations.Test;

import core.Core;
import pages.homepage;

public class LoginasaGuestUser extends Core
{
	/*
	 * @Test public void Guestuserlogin() { try { homepage hmepg = new
	 * homepage(driver); //assertEquals(actual, expected);
	 * hmepg.SearchProduct("Buscopan 10mg"); } catch(Exception e) {
	 * e.printStackTrace(); } }
	 */
}
